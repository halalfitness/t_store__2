import 'package:flutter/material.dart';

class AvailablePlansTab extends StatelessWidget {
  const AvailablePlansTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Available Plans'),
    );
  }
}
