import 'package:flutter/material.dart';

class PrivilegesTab extends StatelessWidget {
  const PrivilegesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Privileges'),
    );
  }
}
