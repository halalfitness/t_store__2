import 'package:flutter/material.dart';

class GuidanceTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Guidance Content'),
    );
  }
}
